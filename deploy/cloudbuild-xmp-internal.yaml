# XMP 内部 API 数据采集 - Cloud Build 配置
steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/xmp-internal-scraper'
      - '-f'
      - 'deploy/Dockerfile.xmp-internal'
      - '.'

images:
  - 'gcr.io/$PROJECT_ID/xmp-internal-scraper'

options:
  logging: CLOUD_LOGGING_ONLY
